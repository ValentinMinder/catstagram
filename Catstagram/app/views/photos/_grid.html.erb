<p>
  <strong><%= @title %>:</strong>
  <ul>
    <% unless @item.photos.any? %>
      No photos yet!
    <% end %>
    <% @item.photos.each do |photo| %>
      <li><%= link_to(image_tag(photo.image_url.thumb.url), photo) %>
    <% end %>
  <ul>
</p>